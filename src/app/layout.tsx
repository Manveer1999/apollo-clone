import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({ subsets: ["latin"] });

export const metadata: Metadata = {
  title: "Apollo247 Clone - Your Health Companion",
  description: "A clone of Apollo247 healthcare platform with doctors listing, online consultation and more.",
  keywords: "healthcare, doctors, online consultation, medicine, apollo247, medical consultation",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  );
}
